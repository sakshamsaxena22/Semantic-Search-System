[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:$PORT --workers 2 --timeout 120 backend.app.main:app"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
